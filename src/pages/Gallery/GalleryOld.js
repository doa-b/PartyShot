import React from 'react';
import withStyles from '@material-ui/core/styles/withStyles'

const styles = theme => ({
    root: {},
});
/**
 * Created by Doa on 1-2-2020.
 */
const GalleryOld = withStyles(styles)(
    ({classes}) => {
        return (
            // column grid met plaatjes. hierin map je <photo>
            // <photo> is grid met xs=12, m=6 l =4 photo met daaronder comment en user
            <>
            </>);
    });

export default GalleryOld;